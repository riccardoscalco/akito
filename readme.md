Default initial configuration for a new visual project.

```
> git clone git@github.com:riccardoscalco/.git
> git remote set-url origin git@github.com:riccardoscalco/<project-name>.git
```

```
> yarn install
> yarn build
> yarn live
```